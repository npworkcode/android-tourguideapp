<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <!-- This is the stroke you want to define -->
    <stroke android:width="@dimen/box_border_size" android:color="@color/red_border"/>

    <!-- Optional, round your corners -->
    <corners android:bottomLeftRadius="@dimen/box_curvature"
        android:topLeftRadius="@dimen/box_curvature"
        android:bottomRightRadius="@dimen/box_curvature"
        android:topRightRadius="@dimen/box_curvature" />

    <!-- Optional, fill the rest of your background with a color or gradient,
    use transparent if you only want the border to be displayed-->
    <gradient android:startColor="@android:color/transparent"
        android:endColor="@android:color/transparent"
        android:angle="90"/>
</shape>